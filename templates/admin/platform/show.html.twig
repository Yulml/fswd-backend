{% extends 'base.html.twig' %}

{% block title %}Platform{% endblock %}

{% block body %}
    <h1>Platform</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ platform.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ platform.name }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ platform.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_platform_index') }}">back to list</a>

    <a href="{{ path('app_admin_platform_edit', {'id': platform.id}) }}">edit</a>

    {{ include('admin/platform/_delete_form.html.twig') }}
{% endblock %}
