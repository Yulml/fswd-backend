{% extends 'base.html.twig' %}

{% block title %}Owned index{% endblock %}

{% block body %}
    <h1>Owned index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for owned in owneds %}
            <tr>
                <td>{{ owned.id }}</td>
                <td>
                    <a href="{{ path('app_admin_owned_show', {'id': owned.id}) }}">show</a>
                    <a href="{{ path('app_admin_owned_edit', {'id': owned.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_owned_new') }}">Create new</a>
{% endblock %}
